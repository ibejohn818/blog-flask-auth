version: '3'
services:
  nginx:
    image: jchflask/proxy
    build:
      context: ./docker/proxy
    ports:
        - 80:80
    volumes:
      - ./src:/app
  flask:
    image: jchflask/flask
    build:
      context: ./docker/flask
    ports:
        - 8001:8001
    volumes:
      - ./api:/api
